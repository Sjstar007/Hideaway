function add_to_cart_One(a,b,c,d,f,g,h,i,j){

	/*let cart = localStorage.getItem("cart");
	if (cart == null) {
		//cart is empty
		let products=[];
		var product={productId : rid,productName :ename,productQuantity: 1,productPrice: price,productoffer : offer,productimage : img,productedt : edt,productedf : edf,productdetil : edetail,productaddr : eaddr}
		products.push(product);
		localStorage.setItem("cart",JSON.stringify(products));
		console.log("product is added for the first time");
		//showToast("Item is Added to cart")

	}  
	else{
		//cart is already present 
		let pcart = JSON.parse(cart);
		let oldproduct = pcart.find((item)=> item.productId == rid)
		alert("Event is already present");
		
	}

	

}

 
// delete Items

 function deleteItemFromCart(id){
	let cart = JSON.parse(localStorage.getItem('cart')); 
	let newcart = cart.filter((item) => item.productid != id)
	localStorage.setItem('cart' , JSON.stringify(newcart))
	updateCart() 
	//showToast("product is removed")
}*/}